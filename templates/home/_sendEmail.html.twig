<form id="email-{{eleve.id}}" method="post" action="{{ path('app_emailrecap', {'id': eleve.id}) }}" >

</form>
 <button class="btn btn-warning" data-toggle="modal" data-target="#exampleModalCenter" onclick="text('{{eleve.email}}','email-{{eleve.id}}')">📧</button>
    <!-- Modal -->

<script>
function text(eleve, idFormulaire){
    const text = document.querySelector('.textModal');
    const titre = document.querySelector('.modal-title');
    const id = document.querySelector('.idFormulaireBouton')

    console.log(eleve)
    text.innerText = "Voulez-vous envoyer un mail de relance à "+ eleve + "?"
    titre.innerText = "Envois mail de relance";
    id.value = idFormulaire;
}

</script>